CLAUDE.md

# Locally fetched XSD releases (via tools/fetch-schema.sh) — not committed,
# the source of truth is the official GitHub release.
.schema-cache/

# Locally fetched Java toolchain jars (via tools/fetch-tools.sh) — external
# dependencies from Maven Central, not repo source.
.lib/

# Generated reports / outputs
*.report.html
report.html
full_report.html
validation_report.xml
out/
